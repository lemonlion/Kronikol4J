package io.kronikol.report.flow;

/** Where the flame chart sits relative to the main diagram in a popup (.NET
 *  {@code InternalFlowFlameChartPosition}). */
public enum InternalFlowFlameChartPosition {
    UNDERNEATH,
    BEHIND_WITH_TOGGLE
}
