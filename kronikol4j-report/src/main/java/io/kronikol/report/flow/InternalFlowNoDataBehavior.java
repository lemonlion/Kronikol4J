package io.kronikol.report.flow;

/** How a segment with no captured spans is shown in a popup (.NET {@code InternalFlowNoDataBehavior}). */
public enum InternalFlowNoDataBehavior {
    SHOW_MESSAGE,
    HIDE_LINK,
    VISUAL_DISTINCTION
}
