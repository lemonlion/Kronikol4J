# Maven Central namespace — io.github.lemonlion is auto-verified via the GitHub account
# (the Java packages + Automatic-Module-Name stay io.kronikol.*).
group=io.github.lemonlion
version=0.1.23-SNAPSHOT

org.gradle.parallel=true
org.gradle.caching=true
